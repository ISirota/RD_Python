# Факторіал

def fact(n):
    # функція обчислення факторіалу
    if n <= 1:
        return 1
    else:
        return (n*fact(n-1))

n = int(input("Ведіть натуральне число, для якого Ви хочете визначити факторіал "))
print(n, '!  = ', fact(n))